# Neovim 实施清单（已完成验收，留档）

生成日期：2026-09-13。来源：[能力 JSON](nvim-capabilities.json)。

本次读取到 **53 项待实施 + 30 项已决定 = 83 项**；另有 5 项待试用、88 项待筛查。插件、窗口、键位、比较组和工具表没有待实施行。本清单仅以能力表的 53 项待实施为实施范围，逐条保留备注原文。

2026-09-14 收尾：本清单 53 项与用户导出的 53 项“已验证”逐一对应，现统一为“已验收”。以下实施过程保留为审阅记录；旧状态说明不代表当前状态，G04 以文末审阅调整为准。

## 本轮流程与约定

1. 清单已经审核通过，已按项目内补充意见实施。
2. 配置与说明已调整，相关检查结果见文末；下面的勾选仅表示已实施。
3. 配置修改完成不等于验收通过；JSON 继续保持待实施。
4. 你明确验收通过后，才更新你通过的 ID，不连带更新其他表或未验收项目。

复选框表示实施完成，不表示你已验收。现有未提交的功能窗口、状态栏和文件树修复要保留。使用 nvim-test 配置验证；普通 nvim 的 Nix 部署不与本次审核默认绑定。此文件是本轮临时执行清单，长期想法仍放在 [TODO.md](TODO.md)。

## 实施顺序与文件归属

| 顺序 | 范围                     | 主要位置                                                                                           |
| ---- | ------------------------ | -------------------------------------------------------------------------------------------------- |
| 1    | 文件、缓冲区、窗口、会话 | config/keymaps.lua；plugins/ui/neotree.lua、snacks.lua、which_key.lua；按插件新增精简覆盖声明      |
| 2    | 代码导航、诊断与 TODO    | plugins/coding/lsp.lua；对应 Trouble、todo-comments、fzf-lua 覆盖；语言专属配置                    |
| 3    | Git                      | Gitsigns、Snacks/FzfLua Git 入口覆盖；plugins/coding/neogit.lua                                    |
| 4    | AI                       | plugins/coding/avante.lua；config/state.lua、toggles.lua；config/ui/statusline.lua 与 lualine 接线 |
| 5    | 交叉检查与说明同步       | config/ui/key_descriptions.lua；docs/keymaps.md、相关 guide、能力表的现状字段；相关 tests          |

以上路径均相对于 lua，docs/tests 除外。插件专属行为放插件声明，通用键放 keymaps，偏好读写与业务开关分离，遵守 [结构约定](structure.md)。不修改已安装插件源码。

删除入口时检查 LazyVim 默认、lazy keys、LSP buffer 映射、插件初始化注册和语言覆盖，避免只删一层后重新出现。仅在备注明确要求的能力上统一 cwd；cwd 在调用时读取。

## 文件（10 项）

- [x] **F01 按文件名查找** — 合并入口

  **备注原文：**

  > 合并为按文件名查找，统一使用 cwd 范围。保留启动页 f 和 leader Space，取消 leader ff/fF，不再单独提供自动推断项目根目录的入口。

  **拟调整与检查：** 将 leader Space 与启动页 f 都接到调用时的 cwd 文件搜索；取消 ff。验证进入子目录、执行 :cd 后，两入口范围一致。

  **实施结果：** <leader><Space>; 启动页 f; :FzfLua files。已完成配置调整，等待你的场景验收。

- [x] **F02 按文件名查找当前目录** — 移除

  **备注原文：**

  > 合并为按文件名查找，统一使用 cwd 范围。保留启动页 f 和 leader Space，取消 leader ff/fF，不再单独提供自动推断项目根目录的入口。

  **拟调整与检查：** 并入 F01，取消 fF；不另设自动推断项目根目录的文件搜索快捷键。与 F01 一起验收。

  **实施结果：** 已并入 F01；无独立快捷键。已完成配置调整，等待你的场景验收。

- [x] **F03 查 Git 跟踪文件** — 移除

  **备注原文：**

  > 不需要单独按 Git 跟踪范围找文件，取消 leader fg；日常统一使用 F01。保留插件命令供偶尔调用。

  **拟调整与检查：** 取消 fg 的快捷键声明，保留 FzfLua Git 文件命令。验证普通文件搜索仍可用。

  **实施结果：** 无独立快捷键；:FzfLua git_files。已完成配置调整，等待你的场景验收。

- [x] **F04 最近文件** — 保留

  **备注原文：**

  > 保留当前工作目录内的最近文件，取消跨项目最近文件入口；常用快捷键统一为 leader fr。
  >
  > 启动页的入口保留

  **拟调整与检查：** fr 和启动页 r 都只列调用时 cwd 内的最近文件，取消 fR；不能在配置加载时固定 cwd。验证切换目录后不混入其他项目文件。

  **实施结果：** <leader>fr; 启动页 r（均为 cwd）。已完成配置调整，等待你的场景验收。

- [x] **F05 查找配置文件** — 移除

  **备注原文：**

  > 移除快捷键入口和启动页入口，配置我是由git管理的持久化配置，不太出现临时修改的情况

  **拟调整与检查：** 删除 fc 和启动页 c 的配置查找入口；保留正常文件搜索。验证启动页菜单说明同步消失。

  **实施结果：** 独立快捷键和启动页入口已移除。已完成配置调整，等待你的场景验收。

- [x] **F06 目录树浏览** — 合并入口

  **备注原文：**

  > 文件树统一使用 cwd 范围，保留 leader e，取消 leader fe/E/fE；重复按 leader e 打开或关闭文件树。

  **拟调整与检查：** 将 e 从 fe 的按键别名改为直接调用 Neo-tree 的 cwd toggle，再删除 fe/E/fE。验证连续按 e 开关、切换 cwd 后目录正确。

  **实施结果：** <leader>e（cwd 开关）; :Neotree filesystem show。已完成配置调整，等待你的场景验收。

- [x] **F08 从文件树选择打开位置** — 合并入口

  **备注原文：**

  > Enter / w合并成Enter

  **拟调整与检查：** 取消 Neo-tree 局部 w，仅 Enter 负责原来的选择窗口打开动作。保留 v/s、现有 l 行为，以及功能窗口过滤、启动页复用和无目标新建逻辑。验收双编辑窗口选择、取消选择、仅功能面板和启动页四种场景。

  **实施结果：** Enter 选择打开；v/s 选择后分屏；l 普通打开；w/S 禁用。已完成配置调整，等待你的场景验收。

- [x] **F09 复制文件位置** — 保留

  **备注原文：**

  > <leader>fL 路径与行号改成小写l

  **拟调整与检查：** 将 fL 的路径与行号动作迁到 fl，保留 fy/fY；同步中文说明。验证相对路径、绝对路径、路径加行号三种输出。

  **实施结果：** <leader>fy 相对路径; fY 绝对路径; fl 路径与行号。已完成配置调整，等待你的场景验收。

- [x] **F11 保存文件** — 合并入口

  **备注原文：**

  > 只保留:w命令
  > 删除ctrl+s

  **拟调整与检查：** 删除普通编辑场景 n/i/x/s 的 Ctrl-s 保存映射，使用 :w 保存。原生 :wall 等命令不封禁；Avante 输入框局部 Ctrl-s 发送不受此项影响。分别验证文件保存与 AI 输入映射。

  **实施结果：** :write; :wall；全局 Ctrl-s 保存已移除。已完成配置调整，等待你的场景验收。

- [x] **F13 系统程序打开文件** — 保留

  **备注原文：**

  > 交换neotree的大小o的功能

  **拟调整与检查：** Neo-tree 当前 O 是系统打开，o 是排序菜单前缀。改为 o 系统打开、O 排序菜单，并将 oc/od/og/om/on/os/ot 等实际存在的排序子键迁到大写 O 前缀，同步菜单 prefix_key，避免残留前缀导致等待。核对各 source 的继承映射；不改无关插件的 o/O。

  **实施结果：** Neo-tree o 系统打开；O 排序菜单，On 等排序子键。已完成配置调整，等待你的场景验收。

## 缓冲区（6 项）

- [x] **B01 缓冲区列表查找** — 合并入口

  **备注原文：**

  > 缓冲区搜索只保留 leader fb，取消 leader ,/fB，按最近使用排序。当前三个入口没有需要保留的范围差异。

  **拟调整与检查：** fb 统一调用按最近使用排序的缓冲区搜索，删除 leader , 与 fB；验证隐藏缓冲区仍可找到，切换后排序更新。

  **实施结果：** <leader>fb（最近使用排序）。已完成配置调整，等待你的场景验收。

- [x] **B02 树状缓冲区浏览** — 移除

  **备注原文：**

  > 不需要侧边缓冲区列表，取消 leader be；Neo-tree 主要负责磁盘目录浏览和文件管理。

  **拟调整与检查：** 删除 be 的侧边缓冲区列表入口；不删除 Neo-tree 或其内部 buffers source。验证文件树和 fb 各自正常。

  **实施结果：** 无独立快捷键；:Neotree buffers show。已完成配置调整，等待你的场景验收。

- [x] **B03 缓冲区标签栏与排序** — 合并入口

  **备注原文：**

  > 保留顶部标签栏、Shift+h/l 切换、字母选择，取消重复的 [b/]b、排序、固定；
  > 字母选择改成leader bp

  **拟调整与检查：** 保留标签栏点击与 Shift+h/l，取消 [b/]b、[B/]B 排序及固定入口；先释放原 bp 固定功能，再将 bj 的字母选择迁到 bp。与 B05 一起清理 bP。验证选择字母不再固定标签。

  **实施结果：** 顶部标签栏；Shift+h/l；<leader>bp 按字母选择。已完成配置调整，等待你的场景验收。

- [x] **B04 上一个缓冲区** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 备注为空：暂按“移除自定义入口”理解，删除 bb 和 leader 反引号；保留原生 Ctrl-^ 与缓冲区机制。若你也要禁用原生 Ctrl-^，需在审核时明确。

  **实施结果：** 原生 Ctrl-^；自定义切换入口已移除。已完成配置调整，等待你的场景验收。

- [x] **B05 关闭与清理缓冲区** — 合并入口

  **备注原文：**

  > 保留bo和bd其他删除

  **拟调整与检查：** 自定义关闭入口仅留 bo（关闭其他）与 bd（关闭当前且保留布局），删除 bD/bi/bl/br/bP。备注“其他删除”暂按包括标签栏鼠标关闭入口处理：隐藏关闭按钮并取消相应关闭动作，保留标签切换；此边界请重点审核。验证未保存文件提示和分屏布局。

  **实施结果：** <leader>bd; <leader>bo；标签栏关闭按钮与鼠标关闭动作已移除。已完成配置调整，等待你的场景验收。

- [x] **B06 原生标签页** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 备注为空：删除 leader Tab 下自定义标签页入口及空分组；不禁用原生 :tab 命令、gt/gT 或插件使用 tabpage 的内部机制。

  **实施结果：** 原生 :tab 命令、gt/gT；leader Tab 分组已移除。已完成配置调整，等待你的场景验收。

## 窗口（4 项）

- [x] **W01 创建关闭与移动分屏** — 合并入口

  **备注原文：**

  > 自定义窗口操作统一到 leader w：ws 上下分屏、wv 左右分屏、wd 关闭窗口。取消 leader - 和 leader |，保留原生 Ctrl-w 系列。

  **拟调整与检查：** 增加 ws 上下分屏、wv 左右分屏，保留 wd；取消 leader - 与 leader |。保留 Ctrl-w 原生系列，验证三个动作与菜单描述。

  **实施结果：** <leader>ws/wv/wd；原生 Ctrl-w 系列。已完成配置调整，等待你的场景验收。

- [x] **W04 缩放当前窗口** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 移除 wm/wM/uZ 缩放入口及本地别名；不删除整个 Snacks。验证这些旧入口不被懒加载重新注册。

  **实施结果：** 缩放自定义入口已移除。已完成配置调整，等待你的场景验收。

- [x] **W05 专注与调暗** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 移除 uz 专注、uD 调暗入口，核对自动启用配置并停止主动启用相关功能；保留其他 Snacks 功能。

  **实施结果：** 专注与调暗自定义入口已移除。已完成配置调整，等待你的场景验收。

- [x] **W06 窗口操作提示循环** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 移除 Ctrl-w Space 的 Which-key 窗口循环操作入口；保留普通 Which-key 提示与 Ctrl-w 原生窗口操作。

  **实施结果：** Ctrl-w Space 入口已移除；普通 Which-key 提示保留。已完成配置调整，等待你的场景验收。

## 会话（3 项）

- [x] **Q01 项目会话保存与恢复** — 调整边界

  **备注原文：**

  > 保留会话自动保存，使用启动页 s 和 leader qs 恢复当前项目会话；取消 leader qS/ql /qd独立快捷键。

  **拟调整与检查：** 保留退出时自动保存与启动页 s、qs 恢复当前项目；删除 qS/ql/qd。验证保存后重启按当前项目恢复；不添加自动启动恢复。

  **实施结果：** 启动页 s；<leader>qs；自动保存。已完成配置调整，等待你的场景验收。

- [x] **Q03 临时笔记与草稿** — 调整边界

  **备注原文：**

  > 入口改为 leader qn 选择已保存的草稿，取消 leader S 保留leader .

  **拟调整与检查：** 将原 S 的“选择已保存草稿”迁到 qn，保留 leader . 打开草稿。保留当前 cwd/分支/文件类型等草稿身份与持久化机制，不额外改成每项目唯一 Markdown。验证重启可选回已保存草稿。

  **实施结果：** <leader>. 打开草稿；<leader>qn 选择已保存草稿。已完成配置调整，等待你的场景验收。

- [x] **Q04 终端** — 调整边界

  **备注原文：**

  > 终端统一使用 cwd；保留 Ctrl-/ 快速打开／收起，保留 Ctrl-\_ 编码兼容映射；取消 leader ft/fT。原生 :terminal 命令继续可用。

  **拟调整与检查：** Ctrl-/ 与 Ctrl-\_ 接到调用时 cwd 的终端开关，删除 ft/fT。保留 :terminal；验证普通模式打开与终端模式收起及目录切换。

  **实施结果：** Ctrl-/；Ctrl-_ 兼容编码；:terminal（Snacks 入口统一 cwd）。已完成配置调整，等待你的场景验收。

## 代码（9 项）

- [x] **C01 定义/引用/实现跳转** — 调整边界

  **备注原文：**

  > > 保留 gd/gr/gI/gy/gD，作为快速代码导航入口；leader k 保留为 Trouble 常驻导航列表。清理重复的 Neovim 默认 LSP 映射 grr/gri/
  > > grt；grn/gra/grx/gO 分别随重命名、代码操作、CodeLens、符号搜索的决定统一处理。

  **拟调整与检查：** 保留 gd/gr/gI/gy/gD；在正确的 LSP/运行时映射层清理 grr/gri/grt，并联动 C03/C04/C05/C08 删除重复 grn/gra/grx/gO。检查相关模式与语言专属覆盖，不删除插件内部 <Plug>。验证 LSP attach 后、重新开文件后不回生。

  **实施结果：** gd/gr/gI/gy/gD；<leader>k 常驻列表；重复默认 LSP 入口已移除。已完成配置调整，等待你的场景验收。

- [x] **C02 悬浮文档与签名** — 调整边界

  **备注原文：**

  > 保留 K 悬浮文档、Noice 自动签名提示和插入模式 Ctrl-k 手动签名；取消普通模式 gK。

  **拟调整与检查：** 保留 K、Noice 自动签名和插入 Ctrl-k，删除普通模式 gK。验证 Python 函数参数处自动提示和手动重新打开签名。

  **实施结果：** K；自动签名；插入 Ctrl-k；Ctrl-f/b 滚动。已完成配置调整，等待你的场景验收。

- [x] **C03 重命名符号与文件** — 调整边界

  **备注原文：**

  > 保留 leader cr 符号重命名，取消 leader cR 的文件重命名入口；文件重命名统一在 Neo-tree 中操作。

  **拟调整与检查：** 保留 cr 符号重命名，删除 cR 文件重命名入口；文件改名由 Neo-tree 完成，并验证 LSP 文件重命名联动。遇到 Rust 对 cR 的不同动作单独核对，不按文件重命名一并误删。

  **实施结果：** <leader>cr 符号重命名；Neo-tree r 文件重命名；Rust 专属 cR 代码动作保留。已完成配置调整，等待你的场景验收。

- [x] **C04 代码操作与整理导入** — 调整边界

  **备注原文：**

  > > 保留 leader ca 代码操作菜单和 leader co 整理导入，取消 leader cA。Python 已验证 ca 包含所需 source 操作；其他语言若发现缺失
  > > 再补充。格式化 leader cf和import整理的co 独立保留，不与代码操作合并。

  **拟调整与检查：** 删除 cA，保留 ca/co/cf，分别负责代码操作、导入整理与格式化。复查 Python ca 可列出所需 source 操作；不要将其他语言能力无条件等同 Python。

  **实施结果：** <leader>ca 代码操作；co 整理导入；cf 格式化；cA 已移除。已完成配置调整，等待你的场景验收。

- [x] **C05 CodeLens** — 调整边界

  **备注原文：**

  > > 保留底栏 CodeLens 开关和 leader cc 执行入口；取消 leader cC 手动刷新，使用 Neovim 原生自动更新。当前 CodeLens 文字不支持鼠
  > > 标点击执行。执行入口的快捷键待定

  **拟调整与检查：** 保留底栏开关与单一执行动作，删除 cC，依赖当前 Neovim CodeLens 自动更新；执行键暂保留 cc，等你确定是否换键。验收启停、编辑后更新与执行，不将虚拟文字当可点击按钮。

  **实施结果：** 底栏 CodeLens 开关；<leader>cc 执行；自动更新。已完成配置调整，等待你的场景验收。

- [x] **C08 文档符号选择** — 移除

  **备注原文：**

  > > 取消文档／工作区符号搜索的独立入口 leader ss/sS，不实现合并列表或范围切换。跨文件定位统一使用项目内容搜索；保留文件大纲和当
  > > 前符号导航。

  **拟调整与检查：** 删除 ss 文档符号搜索和对应默认 gO 独立入口；保留大纲、当前符号导航与项目内容搜索。不关闭 LSP documentSymbol 能力本身。

  **实施结果：** 独立符号搜索入口已移除；保留 C10/C11。已完成配置调整，等待你的场景验收。

- [x] **C09 工作区符号选择** — 移除

  **备注原文：**

  > > 取消文档／工作区符号搜索的独立入口 leader ss/sS，不实现合并列表或范围切换。跨文件定位统一使用项目内容搜索；保留文件大纲和当
  > > 前符号导航。

  **拟调整与检查：** 删除 sS 工作区符号搜索；不开发合并列表或范围切换。与 C08 联合检查快捷键，不停用服务端符号能力。

  **实施结果：** 独立符号搜索入口已移除；保留项目内容搜索。已完成配置调整，等待你的场景验收。

- [x] **C10 符号树** — 调整边界

  **备注原文：**

  > 保留 Trouble 文件大纲，快捷键改为 leader o，取消 leader cs，说明统一为“文件大纲”。

  **拟调整与检查：** 将 Trouble symbols 的 cs 迁到 leader o，描述“文件大纲”。验证大纲跟随文件、功能窗口命名和打开目标过滤。

  **实施结果：** <leader>o（Trouble 文件大纲）。已完成配置调整，等待你的场景验收。

- [x] **C11 LSP 导航汇总列表** — 调整边界

  **备注原文：**

  > 保留 Trouble 当前符号导航列表，用 leader k 打开／关闭，取消 leader cS。说明为“当前符号导航（定义／引用等）”；保留功能面板内
  >
  > > 已有局部映射，不改动 K 和 leader K。

  **拟调整与检查：** 将 Trouble lsp 的 cS 迁到 leader k，描述“当前符号导航（定义／引用等）”。保留 K、leader K 与面板局部键；验证定义与引用列表随当前符号工作。

  **实施结果：** <leader>k（Trouble 当前符号导航）。已完成配置调整，等待你的场景验收。

## 诊断（9 项）

- [x] **D02 当前行诊断详情** — 调整边界

  **备注原文：**

  > > 保留当前行诊断详情浮窗，用于阅读行尾显示不完整的诊断。快捷键由 leader cd 改为 leader xd，说明改成“当前行诊断详情”；浮窗支持
  > > 长消息换行。

  **拟调整与检查：** 将 cd 迁到 xd，显示当前行诊断详情并允许长消息换行，说明同步。用长诊断验证完整阅读；不限制成仅光标所在列。

  **实施结果：** <leader>xd（当前行诊断详情，换行）。已完成配置调整，等待你的场景验收。

- [x] **D03 上下诊断跳转** — 移除

  **备注原文：**

  > 诊断专用的跳转键，例如 [d/]d、[e/]e、[w/]w。

  **拟调整与检查：** 删除诊断专用 [d/]d、[e/]e、[w/]w 等实际映射，包括可能暴露的 Neovim 默认诊断跳转入口；不删除 Quickfix 等其他列表的导航。验证插件加载及 LSP attach 后旧入口仍不出现。

  **实施结果：** 诊断专用跳转键已移除；列表内跳转保留。已完成配置调整，等待你的场景验收。

- [x] **D04 Trouble 诊断列表** — 调整边界

  **备注原文：**

  > Trouble 作为主要诊断列表，保留 leader xx，打开后聚焦。
  > 删除leader xX

  **拟调整与检查：** xx 使用 Trouble diagnostics 列出已收集的诊断，打开后聚焦，删除 xX。不得误改成当前 buffer 过滤；工作区全量程度仍取决于语言服务器实际发布的诊断。验证多文件诊断集中显示与列表内打开文件。本轮按该项现有备注实施，未新增复制导出入口。

  **实施结果：** <leader>xx（Trouble diagnostics，打开后聚焦）。已完成配置调整，等待你的场景验收。

- [x] **D05 搜索诊断** — 移除

  **备注原文：**

  > 取消 FzfLua 诊断快捷键 sd/sD，保留 FzfLua 的其他搜索能力。

  **拟调整与检查：** 删除 sd/sD 的 FzfLua 诊断入口，保留 FzfLua 文件、内容和 LSP 导航等其他搜索。

  **实施结果：** FzfLua 诊断快捷键已移除。已完成配置调整，等待你的场景验收。

- [x] **D06 原生 Quickfix/位置列表** — 调整边界

  **备注原文：**

  > 保留原生 Quickfix／位置列表的数据机制及 :copen、:lopen 命令。leader xq 改为通过 Trouble 打开／关闭 Quickfix，不再打开原生窗口。位置列表不保留专用快捷键。

  **拟调整与检查：** 将 xq 从原生 Quickfix 窗口动作改为 Trouble Quickfix toggle；删除专用位置列表快捷键。保留 Quickfix/loclist 数据机制和 :copen/:lopen，验证其他工具仍可写入列表。

  **实施结果：** :copen/:lopen；<leader>xq 使用 Trouble Quickfix。已完成配置调整，等待你的场景验收。

- [x] **D07 Trouble Quickfix/位置列表** — 调整边界

  **备注原文：**

  > > Quickfix 使用 leader xq 在 Trouble 中打开／关闭，取消 leader xQ。暂不需要位置列表的专用快捷键，取消 leader xL，保留原生命令。

  **拟调整与检查：** 与 D06 共用同一个 xq，删除 xQ/xL 及残留位置列表别名。验证 Trouble 正确读取已有 Quickfix 条目、重复按键可关闭；原生位置列表仍能通过命令查看。

  **实施结果：** <leader>xq；无位置列表专用键。已完成配置调整，等待你的场景验收。

- [x] **D08 搜索 Quickfix/位置列表** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 备注为空：按移除搜索入口处理，删除 sq/sl 的 Quickfix/位置列表搜索键；保留 FzfLua 插件及原生列表数据。

  **实施结果：** FzfLua Quickfix/位置列表搜索快捷键已移除。已完成配置调整，等待你的场景验收。

- [x] **D09 TODO 标记与前后跳转** — 调整边界

  **备注原文：**

  > 代码 TODO 标记统一由 todo-comments 识别、高亮和搜索；结果列表使用 Trouble。基础语法高亮继续保留。
  >
  > 删除[t/]t快捷键

  **拟调整与检查：** 保留 todo-comments 识别、高亮、搜索；删除 [t/]t。保留基础语法高亮，不移除 Tree-sitter 等基础设施。

  **实施结果：** todo-comments 自动识别/高亮；[t/]t 已移除。已完成配置调整，等待你的场景验收。

- [x] **D10 TODO 搜索与常驻列表** — 调整边界

  **备注原文：**

  > TODO 列表统一使用 Trouble，仅保留 leader xt，扫描 cwd 及其子目录中的全部受支持标记。取消 leader xT/st/sT，不增加类别切换操作

  **拟调整与检查：** xt 调用 cwd 下的 Trouble TODO 列表，包含全部支持的标记；删除 xT/st/sT，不添加类别切换。验证子目录、忽略规则与保存后重新打开列表的结果；扫描以磁盘内容为准。

  **实施结果：** <leader>xt（cwd 全部支持标记，Trouble）。已完成配置调整，等待你的场景验收。

## Git（5 项）

- [x] **G02 Lazygit 仓库交互界面** — 有意并存

  **备注原文：**

  > 用户明确要求 Lazygit 与 Neogit 并存，不列入二选一清理。
  > 入口只保留leader gg

  **拟调整与检查：** 仅留 gg 的 Lazygit 快捷键，删除 gG；保留 Neogit 主入口。此备注未要求改变 gg 现有根目录解析，不将文件搜索的 cwd 决定扩散到这里。

  **实施结果：** <leader>gg；与 Neogit gn 并存。已完成配置调整，等待你的场景验收。

- [x] **G03 行级改动与暂存重置** — 调整边界

  **备注原文：**

  > > 普通模式 Alt+j/k 改为下一个／上一个 Git 改动块，取消 ]h/[h。插入、可视及 Select 模式保留原有 Alt+j/k 移动文本行为；普通模式移动行时先按 V 再操作。保留 J
  > > 合并行、K 悬浮文档。仅在适用的 Git 文件缓冲区绑定，保留功能面板的局部操作；实施时核对映射覆盖及 diff 模式行为。

  **拟调整与检查：** 在适用的 Git 文件 buffer 中绑定普通模式 Alt+j/k 跳改动块，删除 [h/]h；保留 i/x/s 的移动文本动作及 J/K。核对 diff 模式行为、功能面板局部覆盖，以及非 Git buffer 的现有行为。用多个改动块验证双向跳转和可视模式移动。

  **实施结果：** 普通 Git 文件 Alt+j/k；<leader>gh 行级操作保留。已完成配置调整，等待你的场景验收。

- [x] **G04 Git 文件/提交/归属搜索** — 调整边界

  **备注原文：**

  > > 仅保留 leader gf（当前文件历史）和 leader gb（当前行历史）。取消 leader gs/gd/gc/gl/gL/gS，仓库状态、差异、提交历史和 stash 统一交给 Neogit／Lazygit。
  > > gf/gb 用于查看历史，实施时将确认动作调整为查看详情，避免直接 checkout。

  **拟调整与检查：** 只留 gf 当前文件历史、gb 当前行历史，删除 gs/gd/gc/gl/gL/gS。将历史选择确认动作改为查看详情，避免 checkout；在临时仓库验证查看历史前后工作区内容和 Git 状态不变。普通 gd 代码导航不受影响。

  **实施结果：** <leader>gf 当前文件历史；gb 当前行历史；Enter 聚焦只读提交详情。已完成配置调整，等待你的场景验收。

- [x] **G05 Git 状态文件树** — 移除

  **备注原文：**

  > 取消 leader ge，Git 改动文件清单交给 Neogit／Lazygit。

  **拟调整与检查：** 删除 ge；核实并记录 Neogit 从文件条目 Enter 回编辑器、Lazygit 文件面板 e 编辑文件的实际效果，满足由客户端进入文件的前提。保留普通文件树。

  **实施结果：** 独立 ge 入口已移除；文件清单交给 Neogit/Lazygit。已完成配置调整，等待你的场景验收。

- [x] **G06 浏览远程代码位置** — 调整边界

  **备注原文：**

  > 远程链接操作改为 leader go 打开、leader gy 复制，支持普通和可视模式。取消原 leader gB/gY，并同步更新 Which-key 中文说明。

  **拟调整与检查：** 将 gB/gY 迁到 go/gy，分别打开与复制远程 URL，支持 n/x；检查选区行号链接和中文说明。普通 gy 类型定义键不变。

  **实施结果：** <leader>go 打开远程链接；gy 复制（n/x）。已完成配置调整，等待你的场景验收。

## AI（7 项）

- [x] **A01 提问与聊天侧栏** — 调整边界

  **备注原文：**

  > 仅保留 leader aa 作为提问及进入 AI 的主入口，取消 leader at/af。沿用 aa 在侧栏内关闭的行为；

  **拟调整与检查：** 保留 aa 提问/进入 AI 及侧栏内关闭行为，删除 at/af 的默认和本地入口；验证编辑区、选区、聊天侧栏三种上下文。

  **实施结果：** <leader>aa 提问/进入侧栏；侧栏内 aa 关闭；命令保留。已完成配置调整，等待你的场景验收。

- [x] **A05 选区与代码面板显示** — 调整边界

  **备注原文：**

  > 功能保留取消快捷键，开关可以加入u系列和底栏切换

  **拟调整与检查：** 删除 aC，保留选区提示功能及侧栏局部代码窗口操作。建议接入 config/toggles.lua 与底栏；u 系列使用 leader uv，不能把“选区提示开关”和侧栏 x 切换代码窗混成一个动作。验证开关显示与实际状态一致。

  **实施结果：** <leader>uv 与底栏：选区提示；侧栏 x 代码窗口。已完成配置调整，等待你的场景验收。

- [x] **A06 模型与提供方** — 调整边界

  **备注原文：**

  > 选模改为 am；通过 config/state.lua 持久化 provider/model，统一插件原有存储。

  **拟调整与检查：** 将 a? 迁到 am，通过 config/state.lua 的偏好 JSON 保存 provider/model；处理 Avante 原有模型持久化的读取优先级与写入路径，避免两份状态互相覆盖。保持 state.lua 只做读写，业务适配放在 AI 配置/辅助模块。验证首次默认、选模后重启、无效旧模型的回退；不存 API key。

  **实施结果：** <leader>am；:AvanteSelectModel；config.state 保存 provider/model。已完成配置调整，等待你的场景验收。

- [x] **A07 ACP 模型/模式入口** — 移除

  **备注原文：**

  > 暂不使用 ACP，取消其 am/aM 映射，为普通选模让出 am。

  **拟调整与检查：** 删除 ACP 的 am/aM 注册，再由 A06 占用 am；核对 Avante 延迟初始化不会注册回来。保留普通 OpenRouter 对话。

  **实施结果：** ACP 的 am/aM 全局入口已移除。已完成配置调整，等待你的场景验收。

- [x] **A08 AI 自动建议** — 保留

  **备注原文：**

  > 需要配置专门的行内建议模型
  > 开关可以加入u系列快捷键和底栏切换

  **拟调整与检查：** 为行内建议配置独立 provider/model，避免聊天 am 切换连带改变建议模型；模型已按补充意见指定。建议开关接入统一 toggles 与底栏，迁移 as 至 leader uV，沿用默认关闭，直到你确认默认值。验证接受键与 Blink 的交互及模型隔离；不靠真实付费请求做基础回归。
  建议模型使用. deepseek/deepseek-v4.1-flash

  **实施结果：** <leader>uV 与底栏开关；Alt-l 等接受/切换建议；独立 OpenRouter deepseek/deepseek-v4.1-flash。已完成配置调整，等待你的场景验收。

- [x] **A09 停止刷新与调试** — 调整边界

  **备注原文：**

  > 只保留 aS 停止；取消 ar/ad/aR 全局入口，维护功能通过命令访问

  **拟调整与检查：** 保留 aS 停止，删除 ar/ad/aR 全局入口；确认维护命令仍可访问。验证取消请求与侧栏正常使用。

  **实施结果：** <leader>aS 停止；维护操作通过命令。已完成配置调整，等待你的场景验收。

- [x] **A10 AI 专注模式** — 移除

  **备注原文：**

  > （空）

  **拟调整与检查：** 删除 az 的 AI 专注模式入口，保留正常侧栏布局与 aa。与 W05 分开处理，避免两个插件的专注模式混淆。

  **实施结果：** az 入口已移除。已完成配置调整，等待你的场景验收。

## 审核时需要明确的几处边界

5. **验收状态名称**：当前 JSON/网页使用“已验证”，没有“已验收”。建议在最终验收阶段将状态名称及网页统计、导入导出与说明统一为“已验收”，再只标记你明确通过的项目；本轮不修改状态体系。

## 实施后的统一验收检查

- 启动和懒加载无错误；普通文件、LSP attach、Neo-tree、Trouble、Avante 各场景分别核对实际键位及中文菜单，检查新键可用、旧入口不回生。
- 文件树执行 tests/neotree_window_picker.lua、tests/utility_windows.lua，保护启动页、无编辑窗口、两个编辑窗口选择及功能面板排除；状态栏原有样式不回退。
- Python 测试项目验证 ca/co/cf、符号重命名、文档和签名；使用临时文件或内存修改，不覆盖测试项目现有内容。
- Trouble 验证跨 buffer 诊断、Quickfix 数据兼容、cwd TODO 和列表跳转。
- 临时 Git 仓库验证 Alt+j/k、历史查看不改工作区、两个客户端回编辑器；远程链接分别核对单行和选区。
- AI 验证模型状态重启恢复、独立建议模型、开关与底栏一致、选区/侧栏入口；运行相关 avante_completion 回归。需要真实服务或手工操作的部分单列给你验收，不声称离线检查覆盖了真实回复。
- 只对涉及的跨插件适配增加必要测试，不为每个普通映射生成机械断言；同步键位与能力现状时保留你的决定、备注和待实施状态。
- 用户验收后再更新 JSON 状态并重新生成 MD/HTML。网页若有旧本地草稿，应重新导入更新后的 JSON，避免旧 localStorage 覆盖新结果。

## 本稿覆盖核验

来源 JSON SHA-256：

`e49139b9c37c8266b66840b0799c8fa4c45d4fd8eaa6c2f07462bf6d551d31a4`

53 个待实施 ID 均有且仅有一个任务复选框；原始备注逐条复制。此摘要用于识别后续 JSON 变化，实施前应重新比较最新数据。

## 实施检查结果（2026-09-13）

- **文件树/功能窗口**：现有两个回归通过；覆盖两个编辑窗口选择、取消选择、无目标新建、启动页复用、Trouble 保留与窗口名显示。o/O 及排序子键合并结果已复核。
- **入口清理**：VeryLazy 和相关插件加载后核对实际映射；清理了删除插件键后重新露出的原生 [B/]B、[t/]t。插入/可视/Select 的 Alt+j/k 保留。
- **Python**：在原测试项目中只修改内存，真实连接 Pyright/Ruff；验证保留的导航、签名映射以及 ca 菜单包含快速修复、Fix all、Organize imports，cA/cR/gK 等旧入口消失。
- **诊断与列表**：临时诊断覆盖两个 buffer，Trouble 集中显示并聚焦；Quickfix 数据能在 Trouble 读取；TODO 扫描包含子目录并遵循 .gitignore。
- **Git**：临时仓库实测 Alt+j/k 双向跳改动块及 diff 分派；真实 gf/gb 对应历史列表的确认只聚焦提交预览，前后 Git 状态与文件内容不变。Neogit 文件 Enter、Lazygit 文件 e 均成功返回当前 Neovim。
- **会话/草稿/终端**：临时会话保存恢复分屏、草稿写入磁盘并出现在草稿列表、cwd 终端打开收起通过。
- **AI**：Avante/Blink 原生模块与补全回归通过；新增 avante_preferences 两次独立启动测试，验证选模保存恢复、旧状态不覆盖、建议模型隔离、开关保存与无关偏好保留。未发送真实模型请求。
- **文档与状态**：所有原有 ID、status、decision、notes 均保留；仅更新现状字段和相关说明。历史键位行用“已移除入口/迁移至”标明，不再当成当前有效映射。JSON 状态仍是待实施。

## 交给你验收

使用 `NVIM_APPNAME=nvim-test nvim` 检查本仓库配置：

1. 文件、缓冲区、窗口与会话：按项目中的保留键体验，重点确认文件树 Enter、bp 字母选择、bd/bo 的关闭行为和终端开关。
2. 代码与诊断：o 文件大纲、k 当前符号导航、xx 诊断、xd 长消息、xq Quickfix、xt TODO；ca/co/cf/cr 在实际语言项目中操作。CodeLens 需支持它的服务器验证 cc 执行动作与自动更新。
3. Git：gg/gn 两个客户端，Alt+j/k，gf/gb 的只读详情及 go/gy 远程链接。远程 URL 的平台格式与桌面浏览器仍请实际确认。
4. AI：aa/an/ae/ah/ac、am 和 aS；uv 选区提示、uV 独立行内建议及两个底栏按钮。建议模型按你填写的 `deepseek/deepseek-v4.1-flash` 配置，账号可用性、实际建议质量与真实请求由你验收。

未更新 Nix 指向，未提交或 push。待你逐项或整体验收通过，再处理第 5 项状态名称和相应 JSON 状态；未通过项目继续修正。

## G04 审阅调整

用户要求历史确认动作改为与当前版本 diff。已将 gf/gb 的 Enter 改为独立标签页左右对比：左侧选中提交中的文件，右侧当前缓冲区快照（包含未保存修改），两边只读，q 关闭对比页。新增 config/git_history.lua，保留工作区与原窗口布局。实测覆盖文件/行历史、文件改名、未保存内容和只读属性；原先“Enter 聚焦提交预览”的实施说明由本项替代。状态仍由用户验收后更新。

## 最终收尾（2026-09-14）

53 项已验收，无遗漏待实施项。已同步 G04 最终 diff 行为、启动页“欢迎”名称、能力表及查看器状态；保留全部原始决定和备注。旧 JSON/浏览器草稿的“已验证”导入时兼容转为“已验收”。

最终回归：功能窗口、Avante 补全、Neo-tree、Git 历史 diff、模型偏好 write/read 均通过。Neo-tree 测试改为验证启动页类型和有名称，不再绑定用户可修改的名称文案。未发送真实 AI 请求。提交、push 与 Nix 锁定更新由最终 Git 记录确认；更新锁定不等于激活系统配置。
